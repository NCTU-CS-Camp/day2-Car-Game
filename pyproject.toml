[project]
name = "day2-car-game"
version = "0.1.0"
description = "A small racing game for teaching Python with pygame."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.1",
]

[tool.uv]
package = false
